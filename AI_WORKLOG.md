# AI Prompt 与问题解决记录

日期：2026-09-15。以下记录本次实际开发过程，由 AI 助手整理；不代表未经用户确认的人工作答或经历。

## 输入与约束

用户原始请求：

> 用飞书 cli 看下文档……从第一性原理出发写一个完备的分析文档

随后请求：

> 请你根据飞书文档和分析文档去实现

使用材料：飞书原题 revision 14 的 CLI 快照，工作区完整分析文档。任务限制：核心 Agent Runtime 自研；真实 LLM API；至少三个 Schema 工具；持久化独立会话；上下文管理与追问；异常、trace、测试及 README。

## 主要决策

| 问题 | 采用的方案 | 取舍与验证 |
|---|---|---|
| 空工作区如何落地 | Python 3.11 + FastAPI + SQLite + 原生 HTTP Provider | FastAPI 只承载 HTTP；核心 loop 自研 |
| 缺少模型与密钥 | 请求用户提供模型配置；实现真实适配器和显式失败 | 生产代码无脚本化模型后备；真实 API 测试独立 opt-in |
| Schema 与工具选择 | 注册表发送名称/描述/JSON Schema，模型返回原生 tool_calls | 工具入口再次验证 JSON 和参数范围 |
| 模型协议如何兼容 | 聚焦 Chat Completions；保留 reasoning_content 续传 | 查阅智谱一手文档；未声称支持 Anthropic 原生协议 |
| 追问依赖哪些信息 | 保留完整模型/工具消息及当前 run | 测试纯对话与工具追问、调用配对 |
| busy 时新消息是否插入 | queued run 先落库，取得 session 锁才追加消息 | FIFO 测试确认无交叉插入；不同 session 并发 |
| 取消/崩溃后的 pending 调用 | 标记未确认并补齐错误结果，不自动重放 | 取消与重启后的协议完整性测试 |
| 基础压缩是否再次调用模型 | 确定性摘录加原始历史回查 | 降低复杂度和额外费用；明确有损 |
| Memory 是否自动抽取 | 仅用户显式保存，按作用域召回 | 用户隔离、更新、删除与无召回测试 |
| 本机页面如何验证 | 实际启动本地服务，使用浏览器检查布局与发送流程 | 发现矮屏初始卡片被挤出可视区，增加紧凑高度布局 |

## 开发中发现并修正的问题

1. 第一轮页面截图在 1280×720 下入口卡片被输入区挤出可视区。调整低高度下的欢迎区间距、字体和卡片高度后重新截图检查。
2. 十进制计算结果默认规范化会显示 `4E+2`。改用普通十进制字符串输出 `400`，便于工具结果阅读；相应修改实际断言。
3. 初始历史回查只包括用户与 assistant，压缩后不利于恢复原始工具数字。扩展为读取普通工具结果，同时排除回查工具自身结果，避免递归膨胀。
4. 防止编辑框发送过程中再次快捷键触发，增加前端提交锁；发送期间用户输入的新内容不被旧请求清空。

## 验证的真实边界

离线模型为测试目录中的 `ScriptedProvider`，只用于确定性复现控制流，应用不导入它。HTTP 适配器测试使用 MockTransport 校验请求与重试。测试报告记录实际测试结果。

本次开始时未发现已配置的模型环境变量，创建了不含密钥的 `.env`。真实模型的准确率、真实工具选择行为、网络延迟和付费账单尚未由离线测试证明。该阶段尚未发布 GitHub。应用本身不具备发送消息给第三方、创建日历或定时调度功能。后续已完成仓库发布并按用户要求公开。

## 真实接口接入后续

用户随后提供了兼容 API 地址和密钥，已仅写入本地 `.env`（权限 0600，Git 忽略）。`/v1/models` 返回 200，从返回模型列表中选择 `gpt-5.4-mini`。实际生成首次测试返回 HTTP 400；随后用基础消息、工具 Schema 和实际 Context 请求分别诊断，均返回 HTTP 429，提示所有可用账户限流。未输出密钥或将其保存到文档。真实测试的最新结果单独记录，未混入离线通过数。

## 指定模型复测成功

用户提出尝试 gpt-5.6-luna / terra；两个模型基础生成均返回 200。随后分别运行现有 live 测试，均通过 calculator 17*23 与同 session 加9追问。说明不能由其他模型限流或不支持推断整个服务不可用。未修改测试断言，默认配置切换 Luna 并重启服务。

## 最终题目验收与打包

补充三个真实场景：文档/搜索与普通追问、双会话/记忆更新、200轮压缩回查/重启。Luna 共4项真实测试全部通过，70项离线测试通过。从白名单生成ZIP并解压到临时目录，新建虚拟环境、按锁文件安装依赖，70项离线测试再次通过。生产测试历史与任何凭证均不入包。

## 对抗式审查与优化（2026-09-16）

用户先要求：

> 接下来我会给你发一个 prompt，记得按照这个 prompt 进行优化项目，并且记录到 ai prompt 里面，并且更新到仓库里

本轮实际 prompt：

> 请你启用对抗式审查，优化项目

执行方式：启用两个独立审查任务，分别攻击运行时/工具边界以及 HTTP/配置/界面状态；主审复现、评估和整合修复，并交叉核验算术改动。只采用有复现证据的问题，没有把猜测计入修复数量。

本轮修复七类问题：整除/取余提前舍入、特殊零值导致过量内存分配、历史尾部不可回查、溢出 JSON 数字进入自定义工具、非 ASCII 令牌触发 500、非有限超时绕过校验，以及活动执行被最近 20 条记录挤出。具体反例、取舍及对应测试见 [对抗式审查记录](docs/ADVERSARIAL_REVIEW.md)。

验证：新增或加强回归后，将测试放到修复前代码上得到 20 failed / 77 passed；修复后 97 项离线测试全部通过。Luna 4 项真实测试全部通过（66.31 秒），其中历史用例加强为压缩后分页找回长消息尾部代号，并验证重启续聊。使用临时数据库，没有修改用户的日常会话。README、测试报告和提交材料同步更新；凭证和本地数据继续排除在仓库与 ZIP 之外。
